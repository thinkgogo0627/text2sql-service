## 실험 1 — Text2SQL 평가 스크립트 수정

아래 3개 파일을 수정해줘.

---

### 1. `mlflow_experiments/text2sql_eval/prompts/brain_v1.yaml` 수정

model 필드를 교체:
- 기존: `Qwen/Qwen2.5-72B-Instruct-Turbo`
- 변경: `meta-llama/Llama-3.3-70B-Instruct-Turbo`

system_prompt에 Few-shot 예시 2개 추가:

```yaml
version: "v1"
model: "meta-llama/Llama-3.3-70B-Instruct-Turbo"
num_examples: 2
description: "DART 재무 도메인 특화 Few-shot 프롬프트"
temperature: 0.1
max_tokens: 1024
system_prompt: |
  너는 DART 재무 데이터 전문 Text2SQL 모델이야.
  아래 스키마와 예시를 참고해서 PostgreSQL 쿼리를 생성해.
  SQL만 반환할 것. 설명 없이.

  [예시 1]
  질문: 삼성전자 2023년 영업이익 알려줘
  SQL: SELECT c.corp_name, f.bsns_year, f.amount
       FROM financial_fact f
       JOIN company_dim c ON f.corp_code = c.corp_code
       WHERE c.corp_name = '삼성전자'
       AND f.account_nm = '영업이익'
       AND f.bsns_year = 2023
       AND f.fs_div = 'CFS'
       AND f.sj_div = 'CIS'

  [예시 2]
  질문: 현대오토에버 최근 3년 영업이익률 추이
  SQL: SELECT f.bsns_year,
       MAX(CASE WHEN f.account_nm = '영업이익' THEN f.amount END) as 영업이익,
       MAX(CASE WHEN f.account_nm = '매출액' THEN f.amount END) as 매출액,
       ROUND(MAX(CASE WHEN f.account_nm = '영업이익' THEN f.amount END) * 100.0
             / NULLIF(MAX(CASE WHEN f.account_nm = '매출액' THEN f.amount END), 0), 2) as 영업이익률
       FROM financial_fact f
       JOIN company_dim c ON f.corp_code = c.corp_code
       WHERE c.corp_name = '현대오토에버'
       AND f.bsns_year >= 2021
       AND f.fs_div = 'CFS'
       GROUP BY f.bsns_year ORDER BY f.bsns_year

  [스키마]
  {schema_context}

  [질문]
  {user_query}
```

---

### 2. `mlflow_experiments/text2sql_eval/eval/test_cases.json` 수정

기존 5개 케이스에 필드 추가하고, 총 30개로 확장해줘.

**추가할 필드:**
- `difficulty`: "easy" | "medium" | "hard"
- `category`: "단순조회" | "기간비교" | "파생지표" | "기업간비교" | "복합조건"
- `expected_values`: [] (일단 빈 리스트 — 나중에 DB 쿼리로 채울 것)
- `sj_div`: "IS" | "CIS" | "BS" | "CF"

**30개 케이스 구성 (난이도 매트릭스):**

Easy 8개 (단순 조회):
- 삼성전자 2023 영업이익
- SK하이닉스 2022 당기순이익
- 현대자동차 2023 매출액
- LG전자 2023 자산총계
- 카카오 2022 부채총계
- POSCO홀딩스 2023 자본총계
- 셀트리온 2023 현금및현금성자산
- KB금융 2023 법인세비용

Medium 8개 (기간 비교):
- 삼성전자 2020~2023 영업이익 추이
- 현대자동차 최근 3년 매출액 추이
- LG화학 2021~2023 당기순이익 변화
- SK하이닉스 2020~2023 자산총계 추이
- NAVER 최근 4년 영업비용 추이
- 기아 2021~2023 유동부채 추이
- 삼성바이오로직스 2020~2023 무형자산 추이
- 신한지주 2021~2023 법인세비용 추이

Medium 6개 (파생 지표):
- 삼성전자 2023 영업이익률 (영업이익/매출액)
- 현대차 2023 부채비율 (부채총계/자본총계)
- LG전자 2023 유동비율 (유동자산/유동부채)
- 카카오 2022 ROE (당기순이익/자본총계)
- SK하이닉스 2023 매출총이익률
- POSCO 2022 자기자본비율 (자본총계/자산총계)

Hard 5개 (기업 간 비교):
- 삼성전자 vs SK하이닉스 2023 영업이익 비교
- 현대차 vs 기아 2022~2023 매출액 비교
- 삼성전자 vs LG전자 2023 부채비율 비교
- KB금융 vs 신한지주 2023 자산총계 비교
- 셀트리온 vs 삼성바이오로직스 2023 무형자산 비교

Hard 3개 (복합 조건):
- 2023년 영업이익 상위 5개사
- 2023년 부채비율 가장 낮은 기업
- 2022~2023년 영업이익 증가율 가장 높은 기업

---

### 3. `mlflow_experiments/text2sql_eval/eval/run_eval.py` 수정

**핵심 변경: keyword_match → DB 실제 실행 기반 평가로 교체**

아래 로직으로 수정해줘:

```python
# 추가 import
from sqlalchemy import text as sa_text
from dotenv import load_dotenv
load_dotenv("infra/.env")

# DB 실행 함수 추가
def execute_sql_on_db(sql: str) -> tuple[list, str]:
    """
    SQL을 실제 PostgreSQL에 실행하고 결과 반환.
    Returns: (result_rows, error_msg)
    """
    try:
        engine = get_engine()
        with engine.connect() as conn:
            result = conn.execute(sa_text(sql))
            rows = [dict(row._mapping) for row in result]
            return rows, ""
    except Exception as e:
        return [], str(e)

# value_match 함수 추가
def value_match(result_rows: list, expected_values: list) -> bool:
    """핵심 수치가 결과에 포함되는지 확인."""
    if not expected_values:
        return True  # expected_values 비어있으면 SQL 실행 성공 여부만 체크
    result_values = []
    for row in result_rows:
        for v in row.values():
            if isinstance(v, (int, float)):
                result_values.append(v)
    return all(v in result_values for v in expected_values)

# run_single_case 수정
async def run_single_case(tc: dict, schema_context: str) -> dict:
    start = time.time()
    sql = await generate_sql(...)
    latency_ms = int((time.time() - start) * 1000)

    valid = is_valid_sql(sql)
    
    # DB 실제 실행
    result_rows, error_msg = execute_sql_on_db(sql) if valid else ([], "Invalid SQL")
    exec_success = len(result_rows) > 0 and not error_msg
    exec_accuracy = value_match(result_rows, tc.get("expected_values", []))

    return {
        "id": tc["id"],
        "difficulty": tc.get("difficulty", ""),
        "user_query": tc["user_query"],
        "generated_sql": sql,
        "valid_sql": valid,
        "exec_success": exec_success,
        "exec_accuracy": exec_accuracy,
        "error_msg": error_msg,
        "latency_ms": latency_ms,
    }

# 메트릭 계산 수정
valid_sql_rate = sum(r["valid_sql"] for r in results) / len(results)
exec_success_rate = sum(r["exec_success"] for r in results) / len(results)
execution_accuracy = sum(r["exec_accuracy"] for r in results) / len(results)
avg_latency = sum(r["latency_ms"] for r in results) / len(results)

# MLflow 메트릭 수정
mlflow.log_metric("execution_accuracy", execution_accuracy)
mlflow.log_metric("valid_sql_rate", valid_sql_rate)
mlflow.log_metric("exec_success_rate", exec_success_rate)
mlflow.log_metric("avg_latency_ms", avg_latency)
```

**주의사항:**
- `pipeline/load/postgres_loader.py`의 `get_engine()` 재사용
- DB 실행 실패 시 skip하고 계속 진행
- POSTGRES_HOST는 localhost, PORT는 5433 (로컬 실행 기준)
- expected_values 비어있으면 exec_success(행 반환 여부)만 체크