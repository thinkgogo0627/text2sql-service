# 2026-03-19 Dev Log

## 수정한 파일 목록

| 파일 | 작업 유형 |
|---|---|
| `pipeline/load/postgres_loader.py` | 버그 수정 |
| `airflow/dags/dart_financial_dag.py` | 버그 수정 |
| `infra/docker-compose.yml` | 버그 수정 |

---

## 각 파일 작업 요약

### `pipeline/load/postgres_loader.py`
- `upsert_financials()`: `engine.begin()` 컨텍스트를 루프 **바깥**에서 한 번만 열던 구조 → **루프 안**으로 이동 (레코드별 독립 트랜잭션)
- 성공/실패 카운트 출력 추가: `Upserted {success}/{len(records)} financial records.`

### `airflow/dags/dart_financial_dag.py`
- `load_task()`: `xcom_pull` 결과가 `None`일 때 `pd.read_json(None)` 으로 크래시되던 문제 수정 → `if not json_str` 체크 추가 후 조기 반환

### `infra/docker-compose.yml`
- `airflow` 웹서버 포트: `8080` → `8088` (로컬 포트 충돌 회피)
- `airflow-init` 서비스에 `POSTGRES_HOST: postgres` 추가 (누락으로 인한 DB 연결 오류 방지)
- `mcp_server` build 경로: `./mcp_server` → `../mcp_server` (infra/ 기준 상대경로 수정)
- `frontend` build 경로: `./frontend` → `../frontend` (동일)

---

## 해결한 버그 목록

| # | 에러/현상 | 원인 | 해결 |
|---|---|---|---|
| 1 | DAG 돌려도 `financial_fact`에 데이터 0건 | `engine.begin()` 하나를 루프 전체가 공유 → 첫 row FK violation 시 PostgreSQL 트랜잭션 `aborted` 상태 전환, 이후 모든 `execute()` 묵살 | `engine.begin()`을 레코드별로 분리 (독립 트랜잭션) |
| 2 | `load_task` XCom None 수신 시 ValueError 크래시 | `pd.read_json(None)` 호출 | `if not json_str` 조기 반환 추가 |
| 3 | Airflow UI 접속 불가 (포트 충돌) | 로컬 8080 포트 점유 | Docker postgres 포트 `8088`로 변경 |
| 4 | `airflow-init` DB 연결 실패 | `POSTGRES_HOST` 미설정으로 `localhost` 시도 | `POSTGRES_HOST: postgres` 추가 |
| 5 | `mcp_server` / `frontend` 빌드 실패 | build 경로가 `infra/` 기준이 아닌 잘못된 상대경로 | `./` → `../` 수정 |

---

## 미완성 / 다음에 이어서 할 것

- [ ] **데이터 적재 최종 확인**: 버그 수정 후 DAG 재실행 → `financial_fact` 건수 검증 (`SELECT COUNT(*) FROM financial_fact`)
- [ ] **카카오뱅크(01133217) 2020~2022 데이터 없음**: 2021년 8월 상장사라 이전 연도 DART 데이터 없을 수 있음. 처리 방식 결정 필요
- [ ] **2026년 데이터 없음**: 사업보고서(11011)는 3~4월 제출이라 2025 결산 데이터도 일부 미제출 상태. 반기보고서(11012) 병행 수집 검토
- [ ] **XCom 크기 제한**: 30개사 × 7년 데이터를 XCom으로 전달 중. 대용량 시 문제 가능 → S3/로컬 파일 경유 방식으로 전환 검토
- [ ] **sentiment_pipeline_dag.py** 미검증
- [ ] **MCP Server / Frontend** 미기동
- [ ] **RAG 파이프라인**: `rag/embedder.py` 아직 미실행
