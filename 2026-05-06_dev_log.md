# 2026-05-06 Dev Log — 실험 1 (Text2SQL) Phase 1 준비

## 작업 목표

실험 1 (Text2SQL 평가) Phase 1 수행을 위한 코드/데이터 준비.
Step 1~4 완료 목표.

---

## Step 1 — test_cases.json 30개 완성

### Claude Code 작업 지시

```
mlflow_experiments/text2sql_eval/eval/test_cases.json을 30개로 확장해줘.

기존 5개 케이스에 아래 필드 추가:
- difficulty: "easy" | "medium" | "hard"
- category: "단순조회" | "기간비교" | "파생지표" | "기업간비교" | "복합조건"
- expected_values: []  ← 빈 리스트 (DB 쿼리로 나중에 채울 것)
- sj_div: "IS" | "CIS" | "BS" | "CF"

30개 구성 (난이도 매트릭스):

Easy 8개 (단순조회):
1.  삼성전자 2023년 영업이익
2.  SK하이닉스 2022년 당기순이익
3.  현대자동차 2023년 매출액
4.  LG전자 2023년 자산총계
5.  카카오 2022년 부채총계
6.  POSCO홀딩스 2023년 자본총계
7.  셀트리온 2023년 현금및현금성자산
8.  KB금융 2023년 법인세비용

Medium 8개 (기간비교):
9.  삼성전자 2020~2023 영업이익 추이
10. 현대자동차 최근 3년 매출액 추이
11. LG화학 2021~2023 당기순이익 변화
12. SK하이닉스 2020~2023 자산총계 추이
13. NAVER 최근 4년 영업비용 추이
14. 기아 2021~2023 유동부채 추이
15. 삼성바이오로직스 2020~2023 무형자산 추이
16. 신한지주 2021~2023 법인세비용 추이

Medium 6개 (파생지표):
17. 삼성전자 2023 영업이익률 (영업이익/매출액)
18. 현대차 2023 부채비율 (부채총계/자본총계)
19. LG전자 2023 유동비율 (유동자산/유동부채)
20. 카카오 2022 ROE (당기순이익/자본총계)
21. SK하이닉스 2023 매출총이익률
22. POSCO홀딩스 2022 자기자본비율 (자본총계/자산총계)

Hard 5개 (기업간비교):
23. 삼성전자 vs SK하이닉스 2023 영업이익 비교
24. 현대차 vs 기아 2022~2023 매출액 비교
25. 삼성전자 vs LG전자 2023 부채비율 비교
26. KB금융 vs 신한지주 2023 자산총계 비교
27. 셀트리온 vs 삼성바이오로직스 2023 무형자산 비교

Hard 3개 (복합조건):
28. 2023년 영업이익 상위 5개사
29. 2023년 부채비율 가장 낮은 기업
30. 2022~2023년 영업이익 증가율 가장 높은 기업

주의사항:
- account_nm은 실제 DB에 존재하는 값만 사용:
  IS/CIS: 매출액, 영업이익, 당기순이익, 매출총이익, 영업비용, 감가상각비
  BS: 자산총계, 부채총계, 자본총계, 자본금, 유동부채, 유형자산, 무형자산,
      현금및현금성자산, 재고자산, 미지급비용, 법인세비용, 법인세부채, 법인세자산
- fs_div는 항상 'CFS' 사용
```

### expected_values 채우기 — DB 쿼리 패턴

Claude Code 작업 완료 후 아래 쿼리 패턴으로 직접 채울 것.

```sql
-- Easy 케이스 (단순 수치 1개)
SELECT f.amount
FROM financial_fact f
JOIN company_dim c ON f.corp_code = c.corp_code
WHERE c.corp_name = '삼성전자'
AND f.account_nm = '영업이익'
AND f.bsns_year = 2023
AND f.fs_div = 'CFS'
AND f.sj_div IN ('IS', 'CIS')
ORDER BY f.sj_div;

-- Medium 기간비교 케이스 (연도별 수치)
SELECT f.bsns_year, f.amount
FROM financial_fact f
JOIN company_dim c ON f.corp_code = c.corp_code
WHERE c.corp_name = '삼성전자'
AND f.account_nm = '영업이익'
AND f.bsns_year BETWEEN 2020 AND 2023
AND f.fs_div = 'CFS'
AND f.sj_div IN ('IS', 'CIS')
ORDER BY f.bsns_year;
```

> ⚠️ 파생지표(tc_017~022), 기업간비교(tc_023~027), 복합조건(tc_028~030)은
> expected_values 빈 리스트로 두고 exec_success(행 반환 여부)만 체크

---

## Step 2 — run_eval.py 수정

### Claude Code 작업 지시

```
mlflow_experiments/text2sql_eval/eval/run_eval.py를 수정해줘.

핵심 변경: keyword_match 기반 평가 → DB 실제 실행 기반 평가로 교체

추가할 함수:

1. execute_sql_on_db(sql: str) -> tuple[list, str]
   - pipeline/load/postgres_loader.py의 get_engine() 재사용
   - SELECT 쿼리만 허용 (보안)
   - POSTGRES_HOST=localhost, PORT=5433 (로컬 실행 기준)
   - 실패 시 ([], error_msg) 반환

2. value_match(result_rows: list, expected_values: list) -> bool
   - expected_values 비어있으면 exec_success만 체크
   - result_rows의 숫자 값들에 expected_values가 모두 포함되는지 확인

run_single_case 수정:
- keyword_match 제거
- DB 실제 실행 추가
- 반환 dict에 exec_success, exec_accuracy, error_msg 추가

메트릭 수정:
- valid_sql_rate: SELECT로 시작하는 비율
- exec_success_rate: DB 실행 후 행 반환 비율
- execution_accuracy: expected_values 일치 비율 (제1 지표)
- avg_latency_ms

MLflow 기록:
- mlflow.set_experiment("text2sql_eval")
- run_name은 CLI 인자로: --run_name
- prompt_version도 CLI 인자로: --prompt_version

results CSV 컬럼:
id, difficulty, category, user_query, generated_sql,
valid_sql, exec_success, exec_accuracy, error_msg, latency_ms

환경변수: load_dotenv("infra/.env")
```

---

## Step 3 — brain_v1.yaml 수정

### Claude Code 작업 지시

```
mlflow_experiments/text2sql_eval/prompts/brain_v1.yaml 수정:

1. model 교체:
   기존: "Qwen/Qwen2.5-72B-Instruct-Turbo"
   변경: "meta-llama/Llama-3.3-70B-Instruct-Turbo"

2. system_prompt에 Few-shot 예시 2개 추가:

예시 1:
질문: 삼성전자 2023년 영업이익 알려줘
SQL: SELECT c.corp_name, f.bsns_year, f.amount
     FROM financial_fact f
     JOIN company_dim c ON f.corp_code = c.corp_code
     WHERE c.corp_name = '삼성전자'
     AND f.account_nm = '영업이익'
     AND f.bsns_year = 2023
     AND f.fs_div = 'CFS'
     AND f.sj_div = 'CIS'

예시 2:
질문: 현대오토에버 최근 3년 영업이익률 추이
SQL: SELECT f.bsns_year,
     MAX(CASE WHEN f.account_nm = '영업이익' THEN f.amount END) as 영업이익,
     MAX(CASE WHEN f.account_nm = '매출액' THEN f.amount END) as 매출액,
     ROUND(MAX(CASE WHEN f.account_nm = '영업이익' THEN f.amount END) * 100.0
           / NULLIF(MAX(CASE WHEN f.account_nm = '매출액' THEN f.amount END), 0), 2) as 영업이익률
     FROM financial_fact f
     JOIN company_dim c ON f.corp_code = c.corp_code
     WHERE c.corp_name = '현대오토에버'
     AND f.bsns_year >= 2021
     AND f.fs_div = 'CFS'
     GROUP BY f.bsns_year ORDER BY f.bsns_year
```

---

## Step 4 — Run A 실행

### 사전 체크리스트

- [ ] docker-compose up 확인 (postgres, mlflow 컨테이너 실행 중)
- [ ] test_cases.json 30개 완성 확인
- [ ] Easy 케이스 expected_values DB 쿼리로 채우기 완료
- [ ] infra/.env에 TOGETHER_API_KEY, POSTGRES_PORT=5433 확인
- [ ] conda activate text2sql-env

### 실행 커맨드

```bash
conda activate text2sql-env
cd ~/text2sql-agent

python mlflow_experiments/text2sql_eval/eval/run_eval.py \
  --run_name brain_fewshot_v1 \
  --prompt_version v1
```

### 결과 확인

```bash
# MLflow UI
http://localhost:5000
→ text2sql_eval 실험 → brain_fewshot_v1 Run

# CSV 결과
cat mlflow_experiments/text2sql_eval/eval/results_v1.csv
```

### 목표 지표

| 지표 | 목표 |
|------|------|
| Execution Accuracy | 85%+ |
| Valid SQL Rate | 95%+ |
| Exec Success Rate | 90%+ |
| Avg Latency | - |

---

## 미완성 / 다음에 할 것

- [ ] Run A 결과 분석 → 어떤 케이스에서 틀리나?
- [ ] brain_v2.yaml 프롬프트 개선 후 Run B 실행
- [ ] generate_synthetic.py 작성 (Phase 2 준비)
- [ ] RAG 인덱서 실행 (rag/embedder.py) — 에이전트 붙이기 전 1회 필요